timestamp: ${now:%Y-%m-%d}T${now:%H-%M-%S}

defaults:
  - _self_
  - paths: pcluster 
  - datamodule: cath_structure_h5
  - hourglass: esmfold
  - latent_scaler: esmfold 
  - trainer: default
  - callbacks/checkpoint: hourglass 
  - logger: hourglass
  - experiment: null


dryrun: False 
run_test: null