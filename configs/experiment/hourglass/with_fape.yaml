# @package _global_

datamodule:
  batch_size: 32
  seq_len: 512
  
hourglass:
  seq_loss_weight: 0.0
  struct_loss_weight: 1.0
  log_sequence_loss: True 
  log_structure_loss: True 

  lr: 1e-4
  lr_sched_type: "constant"

trainer:
  precision: 32
  log_every_n_steps: 5
  limit_val_batches: 4