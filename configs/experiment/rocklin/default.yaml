# @package _global_

defaults:
  - override /datamodule: rocklin_h5.yaml
  - override /callbacks/sample: rocklin.yaml

datamodule:
  batch_size: 64

callbacks/sample:
  batch_size: 64

logger:
  project: plaid_rocklin

denoiser:
  num_blocks: 5

trainer:
  # accumulate_grad_batches: 4
  max_epochs: 8000
  log_every_n_steps: 50