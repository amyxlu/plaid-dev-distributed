# @package _global_

defaults:
  - _self_
  - override /denoiser: UDiT_XXL.yaml
  - override /trainer: ddp.yaml
  - override /callbacks/checkpoint: default.yaml

datamodule:
  batch_size: 2
  max_length: 256

diffusion:
  lr: 6e-4
  lr_num_training_steps: 20_000_000