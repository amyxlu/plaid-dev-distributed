# @package _global_

hourglass_model_id: jzlv54wl
compression_ckpt_dir: ${paths.checkpoint_dir}/hourglass_vq
input_dim: 8 

defaults:
  - override /denoiser: dit.yaml
  - override /datamodule: /compressed/pfam_h5

datamodule:
  hourglass_model_id: ${hourglass_model_id}
  num_workers: 0

callbacks/sample:
  n_to_sample: 640 
  batch_size: 64