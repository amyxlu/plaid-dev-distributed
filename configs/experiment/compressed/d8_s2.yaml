# @package _global_

# downprojection by 8 and shorten by 2
# i.e. 128 x 256

# TODO: make masking for shortened sequences

compression_model_id: 2024-03-17T23-21-19
hid_dim: 128

defaults:
  - override /denoiser: utsa.yaml
  - override /latent_scaler: identity
  - override /datamodule: rocklin/compressed_continuous

datamodule:
  compression_model_id: ${compression_model_id}

denoiser:
  hid_dim: ${hid_dim}
  num_blocks: 3