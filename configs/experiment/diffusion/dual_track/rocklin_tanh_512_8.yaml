# @package _global_

compression_model_id: jzlv54wl
compression_ckpt_dir: ${paths.checkpoint_dir}/hourglass_vq
input_dim: 8 

defaults:
  - override /denoiser: utsa.yaml
  - override /callbacks/sample: rocklin

datamodule:
  compression_model_id: ${compression_model_id}
  num_workers: 0

denoiser:
  input_dim_if_different: ${input_dim}

callbacks/sample:
  n_to_sample: 640 
  batch_size: 64