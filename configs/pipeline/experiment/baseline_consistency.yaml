# @package _global_

samples_dir: /data/lux70/plaid/baselines/protpardelle/samples_large/samples
wandb_job_name: protpardelle_consistency_large 

inverse_generate_structure:
  batch_size: 4 

phantom_generate_structure:
  batch_size: 4 