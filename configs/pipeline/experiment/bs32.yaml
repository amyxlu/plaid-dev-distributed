# @package _global_

defaults:
  - _self_

sample:
  batch_size: 64

decode:
  batch_size: 32 

inverse_generate_structure:
  batch_size: 32 

inverse_generate_sequence:
  batch_size: 1 

phantom_generate_structure:
  batch_size: 32 

